export const EMAIL_DOMAIN = "@dome.tu.ac.th";
export const EMAIL_PATTERN = /^[a-z0-9._%+-]+@dome\.tu\.ac\.th$/;
export const ANY_EMAIL_PATTERN = /^[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$/;
export const MIN_PASSWORD_LENGTH = 8;

/** Temporary: allow any email on login/register while testing verification mail. */
export const ALLOW_ANY_EMAIL = true;
